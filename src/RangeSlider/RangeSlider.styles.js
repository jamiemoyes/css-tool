import styled from "styled-components";

const SliderContainer = styled.div`
  display: grid;
  grid-gap: 1em;
  place-items: start;
  font-size: 0.8em;
`;

const SliderDetails = styled.div`
  display: flex;
  justify-content: space-between;
  width: 100%;
`;

export { SliderContainer, SliderDetails };
